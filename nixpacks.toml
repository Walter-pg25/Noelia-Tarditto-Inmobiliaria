[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['cd backend && npm install --production']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'cd backend && PORT=$PORT node server.js'
